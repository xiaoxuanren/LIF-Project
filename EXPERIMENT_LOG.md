# Experiment Log

Use this file to keep a compact record of model changes, parameter sweeps, and outcomes.

## Entry Template

### YYYY-MM-DD - Short Title
- Goal:
- Hypothesis:
- Files changed:
- Parameters changed:
- Validation run:
- Key result:
- Interpretation:
- Next step:

## Current Focus

### 2026-05-18 - Conductance + Optional h-current Target
- Goal: Continue the project with a conductance-based LIF model and an h-current feature that can be switched on or off as needed.
- Hypothesis: A conductance-based recurrent synapse model with optional h-current will preserve the clustered-network interpretation while supporting cleaner ablation and validation comparisons.
- Files changed: `.github/agents/lif-simulation.agent.md`, `.github/copilot-instructions.md`
- Parameters changed: None yet in simulation code.
- Validation run: Workspace customization files validated without errors.
- Key result: The workspace agent and instructions now encode the conductance-based target and optional h-current requirement.
- Interpretation: Future edits can stay aligned with the intended scientific direction before the repository is cloned into this folder.
- Next step: Clone or copy the repo into this workspace, then implement or verify the conductance synapse path and h-current toggle in the main notebook.

### 2026-05-18 - Short Matched h-current Ablation Pair
- Goal: Generate a matched spontaneous-activity pair with h-current on vs off and summarize the difference from saved sessions.
- Hypothesis: With identical topology and random seed, disabling h-current will reduce sparse spontaneous firing support and alter short-timescale population activity statistics.
- Files changed: `LIF-simulation/compare_h_current_ablation.py`, `LIF-simulation/SCRIPTS_SUMMARY.md`, `LIF-simulation/CLAUDE.md`, `LIF-simulation/DATA_STRUCTURE.md`
- Parameters changed: `n_recordings=1`, `recording_duration=10000`, `num_clusters=20`, `within_cluster_prob=0.3`, `between_cluster_prob=0.15`, `max_connection_distance=6.0`, `cluster_fraction=0.0`, `neurons_per_cluster=0`, `hub_between_prob=0.3`, `use_h_current=True/False`, seed reset to `42` before each run.
- Validation run: Executed a matched short pair using the current conductance notebook code and then ran `compare_h_current_ablation.py` on the two saved sessions.
- Key result: Topology matched exactly. h-current on produced `540` spikes vs `425` without h-current, with mean firing rate `0.181818 Hz` vs `0.143098 Hz`. The off condition showed a higher maximum active fraction in one 100 ms bin (`0.114478` vs `0.063973`).
- Interpretation: In this short spontaneous regime, h-current increased overall sparse firing and active-neuron participation, while the no-h-current run showed one stronger short-timescale co-activity bin despite lower total firing.
- Next step: Repeat the matched pair at longer duration, then compare whether the same direction holds for firing-rate support, active-neuron fraction, and burst-like population excursions.

### 2026-05-18 - 60 s Matched h-current Ablation Pair + Burst Detection
- Goal: Extend the matched spontaneous h-current ablation to 60 seconds, generate raster plots, and detect network bursts from the saved recordings.
- Hypothesis: Over a longer spontaneous run, h-current should continue to support sparse firing without necessarily pushing the network into synchronized auto-bursting under the current conductance and connectivity regime.
- Files changed: `LIF-simulation/compare_h_current_ablation.py`, `LIF-simulation/SCRIPTS_SUMMARY.md`, `LIF-simulation/CLAUDE.md`, `LIF-simulation/README.md`
- Parameters changed: `n_recordings=1`, `recording_duration=60000`, `num_clusters=20`, `within_cluster_prob=0.3`, `between_cluster_prob=0.15`, `max_connection_distance=6.0`, `cluster_fraction=0.0`, `neurons_per_cluster=0`, `hub_between_prob=0.3`, `use_h_current=True/False`, seed reset to `42` before each run. Burst detection used `activity_bin_ms=100`, `smooth_bins=3`, `threshold=max(mean+3*std, 0.10)`, `min_burst_duration_ms=100`, `merge_gap_ms=150`, `pad_before_ms=100`, `pad_after_ms=250`.
- Validation run: Ran the updated `compare_h_current_ablation.py` on the existing short pair to verify raster and burst outputs, then executed a new matched 60 s pair and compared the resulting sessions with raster export and burst detection enabled.
- Key result: Topology matched exactly. The 60 s h-current-on session (`20260518_184000`) produced `3175` spikes and mean firing rate `0.178171 Hz`, while the matched h-current-off session (`20260518_190548`) produced `2155` spikes and mean firing rate `0.120932 Hz`. Neither session crossed the default burst-detection threshold, so detected network-burst count remained `0` in both conditions. Raster plots and `ablation_report_60s.json` were saved under `LIF-simulation/LIF data/h_current_ablation_60s/`.
- Interpretation: At the current spontaneous operating point, h-current increases sparse firing support and active-neuron participation over 60 seconds, but the network still remains below the default network-burst criterion. The off condition showed a slightly larger single-bin maximum active fraction, yet not enough sustained synchrony to count as a detected burst.
- Next step: If burst-like spontaneous events are desired, probe the threshold against longer runs or retune the spontaneous operating point, hub connectivity, or recurrent excitation while keeping matched-seed comparisons.

### 2026-05-26 - All-Recordings Burst Inclusion Comparison For 20260521_130004
- Goal: Compare all-recordings spike-only learned-LIF connectivity performance with detected burst windows included versus excluded on session `20260521_130004`.
- Hypothesis: Excluding detected network-burst windows should reduce burst-driven false positives and improve thresholded edge-call quality, even if ranking AUC does not decrease.
- Files changed: `LIF-simulation/learned_lif_outputs/learned_lif_20260521_130004_cmp1200s_spike_postcentered_k100_exclbursts_allrec.pt`, `LIF-simulation/learned_lif_outputs/connectivity_20260521_130004_cmp1200s_spike_postcentered_k100_exclbursts_allrec_surrogate_fdr.npz`, `LIF-simulation/learned_lif_outputs/learned_lif_20260521_130004_cmp1200s_spike_postcentered_k100_inclbursts_allrec.pt`, `LIF-simulation/learned_lif_outputs/connectivity_20260521_130004_cmp1200s_spike_postcentered_k100_inclbursts_allrec.npz`
- Parameters changed: `session=20260521_130004`, all `20` recordings (`1200 s` total), `K=100`, `epochs=6`, `batch=128`, `max_delay=8`, `l1=0.01`, `pos_weight=5.0`, `dt=1.0`, `candidate_mode=hybrid`, `candidate_spatial_frac=0.8`, `candidate_min_lag=1`, `candidate_max_lag=8`, `threshold_mode=adaptive`, `event_anchor_mode=post`, `surrogate_fdr=0.005`, `n_threshold_surrogates=4`, `surrogate_epochs=2`, `surrogate_patience=1`, comparison arm toggled `--exclude-detected-bursts` on vs off.
- Validation run: Executed matched all-recordings included-burst and excluded-burst fits, then compared saved oracle and surrogate outputs from both checkpoints.
- Key result: Excluding detected burst windows removed `53,259` bins across `91` windows and clearly improved thresholded connectivity quality. Oracle: excluded `F1=0.6792`, `precision=0.7736`, `recall=0.6054`, `AP=0.6880`, `AUC=0.8520`, `selected_edges=1983`; included `F1=0.4799`, `precision=0.4933`, `recall=0.4672`, `AP=0.5089`, `AUC=0.8602`, `selected_edges=2384`. Surrogate: excluded `F1=0.6728`, `precision=0.8007`, `recall=0.5801`, `estimated_FDR=0.00490`, `selected_edges=1836`; included `F1=0.4774`, `precision=0.5113`, `recall=0.4478`, `estimated_FDR=0.00499`, `selected_edges=2204`.
- Interpretation: Including burst periods slightly improved ranking AUC but materially harmed AP, F1, and precision, indicating that burst-dominated windows inflate scores for many candidate edges without improving thresholded edge calls.
- Next step: Use the burst-excluded all-recordings output as the preferred comparison baseline for downstream threshold analysis, degree summaries, and related connectivity reporting.

### 2026-05-26 - All-Recordings Surrogate-FDR Baseline For 20260523_084407
- Goal: Run the spike-only learned-LIF all-recordings fit on session `20260523_084407` using the non-leaky `surrogate_fdr` threshold mode with detected network-burst windows excluded.
- Hypothesis: On this higher-activity session, burst-excluded all-recordings fitting plus surrogate-FDR calibration should preserve strong edge ranking while yielding a conservative high-precision thresholded connectivity baseline without label leakage in threshold selection.
- Files changed: `LIF-simulation/learned_lif_outputs/learned_lif_20260523_084407_cmp1200s_spike_postcentered_k100_exclbursts_allrec.pt`, `LIF-simulation/learned_lif_outputs/connectivity_20260523_084407_cmp1200s_spike_postcentered_k100_exclbursts_allrec.npz`, `LIF-simulation/learned_lif_outputs/learned_lif_20260523_084407_cmp1200s_spike_postcentered_k100_exclbursts_allrec_surrogate_fdr.png`, `LIF-simulation/learned_lif_outputs/learned_lif_20260523_084407_cmp1200s_spike_postcentered_k100_exclbursts_allrec_oracle_f1.png`
- Parameters changed: `session=20260523_084407`, all `20` recordings (`1200 s` total), `K=100`, `epochs=6`, `batch=128`, `max_delay=8`, `l1=0.01`, `pos_weight=5.0`, `dt=1.0`, `candidate_mode=hybrid`, `candidate_spatial_frac=0.8`, `candidate_min_lag=1`, `candidate_max_lag=8`, `threshold_mode=adaptive`, `event_anchor_mode=post`, `connectivity_threshold_mode=surrogate_fdr`, `surrogate_fdr=0.005`, `n_threshold_surrogates=4`, `surrogate_epochs=2`, `surrogate_patience=1`, `--exclude-detected-bursts`.
- Validation run: Executed the all-recordings spike-only learned-LIF CLI on CPU with held-out-recordings validation (`16` train, `4` val), excluded all saved stimulation-onset bins plus detected burst windows, then read the saved checkpoint metrics for both surrogate and oracle threshold reports.
- Key result: The run excluded `60,353` bins total (`100` saved stimulation-onset bins plus `97` detected burst windows), reached held-out window loss `0.1644`, and produced strong ranking quality (`AUC=0.8792`, `AP=0.7578`). The primary non-leaky surrogate threshold was `0.1955`, with `estimated_FDR=0.00490`, `F1=0.7249`, `precision=0.9233`, `recall=0.5967`, and `selected_edges=1734` (`TP=1601`, `FP=133`, `FN=1082`). For retrospective reference, the oracle figure threshold was `0.1682`, yielding `F1=0.7461`, `precision=0.8492`, `recall=0.6653`, and `selected_edges=2102`.
- Interpretation: Excluding detected burst windows still leaves enough data to fit a strong all-recordings model on `20260523_084407`, and the surrogate-FDR calibration behaves as intended: the selected threshold stays near the target null rate while producing a much more precision-focused edge set than the oracle-F1 operating point.
- Next step: If you want a within-session burst comparison for `20260523_084407`, run the matched included-burst arm with the same hyperparameters and compare its surrogate-threshold precision, recall, and edge count against this burst-excluded baseline.

### 2026-05-26 - K150 Follow-Up For 20260523_084407 Burst-Excluded All-Recordings Run
- Goal: Repeat the same burst-excluded, non-leaky all-recordings spike-only learned-LIF run on session `20260523_084407` with `K` increased from `100` to `150`.
- Hypothesis: Raising `K` should improve candidate-edge coverage and may slightly improve ranking AUC, but it could also dilute thresholded edge quality if the extra candidates introduce more near-null scores.
- Files changed: `LIF-simulation/learned_lif_outputs/learned_lif_20260523_084407_cmp1200s_spike_postcentered_k150_exclbursts_allrec.pt`, `LIF-simulation/learned_lif_outputs/connectivity_20260523_084407_cmp1200s_spike_postcentered_k150_exclbursts_allrec.npz`, `LIF-simulation/learned_lif_outputs/learned_lif_20260523_084407_cmp1200s_spike_postcentered_k150_exclbursts_allrec_surrogate_fdr.png`, `LIF-simulation/learned_lif_outputs/learned_lif_20260523_084407_cmp1200s_spike_postcentered_k150_exclbursts_allrec_oracle_f1.png`
- Parameters changed: matched the earlier `20260523_084407` burst-excluded all-recordings run except `K=150` instead of `K=100`; all other settings were unchanged: `epochs=6`, `batch=128`, `max_delay=8`, `l1=0.01`, `pos_weight=5.0`, `dt=1.0`, `candidate_mode=hybrid`, `candidate_spatial_frac=0.8`, `candidate_min_lag=1`, `candidate_max_lag=8`, `threshold_mode=adaptive`, `event_anchor_mode=post`, `connectivity_threshold_mode=surrogate_fdr`, `surrogate_fdr=0.005`, `n_threshold_surrogates=4`, `surrogate_epochs=2`, `surrogate_patience=1`, `--exclude-detected-bursts`.
- Validation run: Executed the matched all-recordings CLI on CPU, then loaded the saved checkpoint with `weights_only=False` to read the stored surrogate and oracle threshold metrics.
- Key result: Increasing `K` raised candidate coverage from `96.5%` to `98.7%` and slightly improved ranking AUC (`0.8824` vs `0.8792`), but thresholded quality did not improve. Surrogate-FDR at `K=150`: threshold `0.2042`, `estimated_FDR=0.00488`, `AP=0.7433`, `F1=0.7144`, `precision=0.9368`, `recall=0.5774`, `selected_edges=1692` (`TP=1585`, `FP=107`, `FN=1160`). By comparison, the earlier `K=100` run had `AP=0.7578`, `F1=0.7249`, `precision=0.9233`, `recall=0.5967`, and `selected_edges=1734`. Oracle at `K=150` was also slightly weaker than `K=100` (`F1=0.7420` vs `0.7461`).
- Interpretation: The larger candidate set gave a small ranking benefit and a slightly more conservative high-precision surrogate threshold, but it reduced AP, recall, and F1. For this burst-excluded all-recordings session, `K=150` does not outperform the existing `K=100` baseline on thresholded connectivity quality.
- Next step: Keep `K=100` as the preferred burst-excluded baseline for `20260523_084407` unless a downstream analysis specifically values the extra candidate coverage or the slightly higher surrogate-threshold precision.

### 2026-05-27 - Both-Anchor Follow-Up For 20260523_084407 Burst-Excluded All-Recordings Run
- Goal: Test whether switching the spike-only event-window anchor from `post` to `both` can reduce false negatives on session `20260523_084407` without materially increasing false positives relative to the matched burst-excluded `K=100` surrogate-FDR baseline.
- Hypothesis: Adding pre-event context should help recover missed inhibitory or low-rate edges and lower `FN`, while keeping `FP` near the existing high-precision baseline if score ranking remains clean.
- Files changed: `LIF-simulation/learned_lif_outputs/learned_lif_20260523_084407_cmp1200s_spike_both_k100_exclbursts_allrec.pt`, `LIF-simulation/learned_lif_outputs/connectivity_20260523_084407_cmp1200s_spike_both_k100_exclbursts_allrec.npz`, `LIF-simulation/learned_lif_outputs/learned_lif_20260523_084407_cmp1200s_spike_both_k100_exclbursts_allrec_surrogate_fdr.png`, `LIF-simulation/learned_lif_outputs/learned_lif_20260523_084407_cmp1200s_spike_both_k100_exclbursts_allrec_oracle_f1.png`
- Parameters changed: matched the earlier `20260523_084407` burst-excluded all-recordings `K=100` spike-only run exactly except `event_anchor_mode=both` instead of `event_anchor_mode=post`; all other settings were unchanged: `epochs=6`, `batch=128`, `max_delay=8`, `l1=0.01`, `pos_weight=5.0`, `dt=1.0`, `candidate_mode=hybrid`, `candidate_spatial_frac=0.8`, `candidate_min_lag=1`, `candidate_max_lag=8`, `threshold_mode=adaptive`, `connectivity_threshold_mode=surrogate_fdr`, `surrogate_fdr=0.005`, `n_threshold_surrogates=4`, `surrogate_epochs=2`, `surrogate_patience=1`, `--exclude-detected-bursts`.
- Validation run: Executed the matched all-recordings CLI on CPU, confirmed the run completed successfully in `496 s`, and loaded both the saved `both`-anchor checkpoint and the earlier `post`-anchor checkpoint with `weights_only=False` for a direct surrogate-threshold comparison.
- Key result: The `both`-anchor run more than doubled the selected edge set and sharply increased recall, but it did not preserve the baseline's precision. Event-window counts rose from `173,230/43,476` train/val in the earlier `post` run to `322,440/76,844`. Ranking AUC improved slightly (`0.8829` vs `0.8792`), but `AP` fell (`0.7380` vs `0.7578`) and the surrogate-threshold operating point became much denser: threshold `0.1826`, `estimated_FDR=0.00499`, `F1=0.6264`, `precision=0.5493`, `recall=0.7287`, `selected_edges=3559` (`TP=1955`, `FP=1604`, `FN=728`). Relative to the `post` baseline, this changed `TP` by `+354`, `FP` by `+1471`, and `FN` by `-354`.
- Interpretation: `event_anchor_mode=both` does recover many edges that the `post`-centered model misses, but on this session it does so mainly by moving to a much broader thresholded prediction set. That misses the stated goal of reducing `FN` while keeping `TP` and `FP` near their previous levels; the false-positive explosion is too large for this to replace the high-precision `post` baseline.
- Next step: Keep `event_anchor_mode=post` as the preferred spike-only burst-excluded baseline for `20260523_084407`. If the goal remains lower `FN` at roughly similar `FP`, the next better-matched comparison is to try voltage-augmented learned-LIF or a more targeted thresholding scheme rather than the broader `both`-anchor spike-only windows.

### 2026-05-27 - Voltage-Augmented Follow-Up For 20260523_084407 Burst-Excluded All-Recordings Run
- Goal: Test whether the matched voltage-augmented learned-LIF pipeline can reduce false negatives on session `20260523_084407` more cleanly than the spike-only alternatives while keeping the same burst-excluded, all-recordings, non-leaky `surrogate_fdr` comparison setup.
- Hypothesis: Adding cleaned subthreshold voltage supervision should improve ranking quality and recover some missed true edges with a smaller false-positive penalty than the broader spike-only `both`-anchor comparison.
- Files changed: `LIF-simulation/voltage_augmented_learned_lif_outputs/voltage_augmented_learned_lif_20260523_084407_cmp1200s_voltage_postcentered_k100_exclbursts_allrec.pt`, `LIF-simulation/voltage_augmented_learned_lif_outputs/connectivity_20260523_084407_cmp1200s_voltage_postcentered_k100_exclbursts_allrec.npz`, `LIF-simulation/voltage_augmented_learned_lif_outputs/voltage_augmented_learned_lif_20260523_084407_cmp1200s_voltage_postcentered_k100_exclbursts_allrec.png`
- Parameters changed: matched the earlier `20260523_084407` burst-excluded all-recordings `K=100` `post`-anchor spike-only run on `epochs=6`, `batch=128`, `max_delay=8`, `l1=0.01`, `pos_weight=5.0`, `dt=1.0`, `candidate_mode=hybrid`, `candidate_spatial_frac=0.8`, `candidate_min_lag=1`, `candidate_max_lag=8`, `threshold_mode=adaptive`, `connectivity_threshold_mode=surrogate_fdr`, `surrogate_fdr=0.005`, `n_threshold_surrogates=4`, `surrogate_epochs=2`, `surrogate_patience=1`, and `--exclude-detected-bursts`, while switching to the voltage-augmented pipeline with `voltage_lambda=1.0` and reset-trace cleaning `mask_pre_ms=1.0`, `mask_post_ms=2.0`.
- Validation run: First validated that the voltage-augmented loader could downsample this full-dt voltage session to `dt=1.0` after native-rate masking, then executed the matched all-recordings CLI on CPU and loaded both the saved spike-only and voltage-augmented checkpoints with `weights_only=False` for a direct surrogate-threshold comparison.
- Key result: Voltage augmentation improved ranking AUC but did not beat the spike-only baseline on thresholded precision-focused quality. The run loaded all `20` recordings (`1200 s` total), excluded the same `60,353` bins, and used the same held-out-recordings validation split (`16` train, `4` val). At the saved surrogate threshold, voltage augmentation reached `AUC=0.9025`, `AP=0.7299`, `F1=0.7079`, `precision=0.7524`, `recall=0.6683`, `selected_edges=2383`, `threshold=0.2127`, and `estimated_FDR=0.00493`, with `TP=1793`, `FP=590`, and `FN=890`. Relative to the spike-only `post` baseline, this changed `TP` by `+192`, `FP` by `+457`, and `FN` by `-192`. The voltage run also reported strong signed-weight recovery (`sign_accuracy=0.9564`, `weight_corr=0.7484`).
- Interpretation: Voltage augmentation is a better FN-reduction direction than the spike-only `both`-anchor change: it raises recall and ranking AUC with a much smaller false-positive increase than `both`. But it still does not keep `FP` near the original spike-only high-precision baseline, and its `AP` and `F1` remain slightly worse than the original spike-only `post` run. For the exact goal of lowering `FN` while keeping `TP` and `FP` close to baseline, this is an improvement in direction, not a complete win.
- Next step: If the goal remains "fewer FN with FP closer to the spike-only baseline," keep the voltage-augmented path and tune the operating point rather than the spike-only anchor. The next best local comparisons are a `voltage_lambda` sweep and/or a slightly stricter surrogate target on this same burst-excluded all-recordings setup.

### 2026-05-29 - Spike-Only K100 Burst-Excluded Baseline For 20260529_025936
- Goal: Run the spike-only learned-LIF all-recordings baseline on the new session `20260529_025936` using the updated default configuration (`K=100`, detected-burst exclusion on) with non-leaky `surrogate_fdr` thresholding.
- Hypothesis: The same burst-excluded `post`-anchor `K=100` workflow used on the earlier all-recordings baselines should provide a conservative high-precision connectivity estimate on the shorter `20260529_025936` session without leaking labels into threshold selection.
- Files changed: `LIF-simulation/lif_inference/learned_lif_connectivity.py`, `LIF-simulation/README.md`, `LIF-simulation/SCRIPTS_SUMMARY.md`, `LIF-simulation/docs/SCRIPT_DETAILS.md`, `LIF-simulation/learned_lif_outputs/learned_lif_20260529_025936_cmp540s_spike_postcentered_k100_exclbursts_allrec.pt`, `LIF-simulation/learned_lif_outputs/connectivity_20260529_025936_cmp540s_spike_postcentered_k100_exclbursts_allrec.npz`, `LIF-simulation/learned_lif_outputs/learned_lif_20260529_025936_cmp540s_spike_postcentered_k100_exclbursts_allrec_surrogate_fdr.png`, `LIF-simulation/learned_lif_outputs/learned_lif_20260529_025936_cmp540s_spike_postcentered_k100_exclbursts_allrec_oracle_f1.png`
- Parameters changed: `session=20260529_025936`, all `9` recordings (`540 s` total), `K=100`, `epochs=40`, `batch=128`, `max_delay=8`, `l1=0.01`, `val_fraction=0.2`, `candidate_mode=hybrid`, `candidate_spatial_frac=0.8`, `threshold_mode=adaptive`, `connectivity_threshold_mode=surrogate_fdr`, `surrogate_fdr=0.005`, `n_threshold_surrogates=4`, `surrogate_epochs=2`, `surrogate_patience=1`, `event_anchor_mode=post`, and `--exclude-detected-bursts`.
- Validation run: Executed the spike-only all-recordings CLI on CPU with held-out-recordings validation (`7` train, `2` val), excluding `55,804` bins total across `18` saved stimulation onsets and `96` detected burst windows. Candidate coverage reached `2610/2841 = 91.9%`. Training early-stopped at epoch `28`, then completed the full `4`-surrogate null calibration and wrote the checkpoint, connectivity export, and both thresholded summary figures.
- Key result: Held-out window loss reached `0.1871` (`spike_loss=0.1863`). The primary non-leaky surrogate threshold was `0.3716`, with `estimated_FDR=0.00494`, `AUC=0.7966`, `AP=0.4077`, `F1=0.3122`, `precision=0.7316`, `recall=0.1985`, and `selected_edges=708` (`TP=518`, `FP=190`, `FN=2092`). For retrospective reference, the oracle threshold was `0.2344`, yielding `F1=0.4145`, `precision=0.4240`, `recall=0.4054`, and `selected_edges=2495` (`TP=1058`, `FP=1437`, `FN=1552`).
- Interpretation: On `20260529_025936`, the burst-excluded `K=100` spike-only baseline remains conservative in the intended non-leaky sense, but ranking quality and especially surrogate-threshold recall are substantially weaker than on the stronger `20260523_084407` session. The surrogate calibration behaved as intended and held the estimated null selection rate near target, but it selected a much sparser edge set and left many false negatives.
- Next step: Keep this run as the default-config baseline for `20260529_025936`. If the next goal is fewer false negatives on this session, the most local follow-up is to compare a voltage-augmented fit and/or a less conservative operating-point sweep rather than relaxing the default burst-excluded `K=100` baseline itself.

### 2026-05-29 - No-Mask-Pre + Longer-Warmup Voltage-Augmented Follow-Up For 20260523_084407
- Goal: Test whether step-1 voltage-timing changes (stop masking the pre-spike depolarization ramp, lengthen per-window warmup) improve postsynaptic-spike-driven connectivity recovery on session `20260523_084407` without leaking labels into threshold selection.
- Hypothesis: Keeping the pre-spike voltage upswing as a supervised target and giving slow membrane/adaptation state a longer lead-in should sharpen spike-timing supervision, raising recall and reducing false negatives at a similar non-leaky surrogate operating point relative to the earlier `mask_pre=1.0`, `warmup=30` voltage baseline.
- Files changed: `LIF-simulation/lif_inference/voltage_augmented_learned_lif_connectivity.py`, `LIF-simulation/scripts/run_voltage_lambda_sweep.py`, `LIF-simulation/SCRIPTS_SUMMARY.md`, `LIF-simulation/voltage_augmented_learned_lif_outputs/voltage_augmented_learned_lif_20260523_084407_cmp1200s_voltage_postcentered_k100_exclbursts_allrec_nomaskpre.pt`, `LIF-simulation/voltage_augmented_learned_lif_outputs/connectivity_20260523_084407_cmp1200s_voltage_postcentered_k100_exclbursts_allrec_nomaskpre.npz`, `LIF-simulation/voltage_augmented_learned_lif_outputs/voltage_augmented_learned_lif_20260523_084407_cmp1200s_voltage_postcentered_k100_exclbursts_allrec_nomaskpre.png`, `LIF-simulation/voltage_augmented_learned_lif_outputs/voltage_augmented_learned_lif_20260523_084407_cmp1200s_voltage_postcentered_k100_exclbursts_allrec_nomaskpre_score_separation.png`
- Parameters changed: matched the earlier `20260523_084407` burst-excluded all-recordings `K=100` `post`-anchor voltage-augmented run on `epochs=6`, `batch=128`, `max_delay=8`, `l1=0.01`, `pos_weight=5.0`, `dt=1.0`, `candidate_mode=hybrid`, `candidate_spatial_frac=0.8`, `candidate_min_lag=1`, `candidate_max_lag=8`, `threshold_mode=adaptive`, `connectivity_threshold_mode=surrogate_fdr`, `surrogate_fdr=0.005`, `n_threshold_surrogates=4`, `surrogate_epochs=2`, `surrogate_patience=1`, `voltage_lambda=1.0`, `--exclude-detected-bursts`, while changing the new step-1 defaults `mask_pre_ms` from `1.0` to `0.0` and `warmup` from `30` to `100` (`pre_context=50`, `post_context=10`, `mask_post_ms=2.0` unchanged).
- Validation run: Executed the voltage-augmented all-recordings CLI on CPU with held-out-recordings validation (`16` train, `4` val), excluding the same `60,353` bins (`100` saved stimulation onsets plus `97` detected burst windows), then read the saved checkpoint `results_all` for the non-leaky surrogate operating point.
- Key result: Loaded all `20` recordings (`1200 s`), candidate coverage `2683/2780 = 96.5%`, held-out window loss `0.7716` (`spike=0.1659`, `voltage=0.6050`). At the surrogate threshold `0.2147` (`estimated_FDR=0.00491`), the run reached `AUC=0.9040`, `AP=0.7331`, `F1=0.7089`, `precision=0.7433`, `recall=0.6776`, `selected_edges=2446`, `TP=1818`, `FP=628`, `FN=865`, `sign_accuracy=0.9665`, `weight_corr=0.7495`. Relative to the prior `mask_pre=1.0`, `warmup=30` voltage baseline (`AUC=0.9025`, `AP=0.7299`, `F1=0.7079`, `precision=0.7524`, `recall=0.6683`, `TP=1793`, `FP=590`, `FN=890`), this changed `TP` by `+25`, `FP` by `+38`, `FN` by `-25`, with small gains in `AUC`, `AP`, `recall`, `sign_accuracy`, and `weight_corr`.
- Interpretation: The step-1 timing changes produced a small, directionally favorable shift on this strong session: slightly higher ranking quality and recall and a modest false-negative reduction, at the cost of a comparable small false-positive increase, so net F1 and precision are essentially unchanged. The effect is consistent with restoring pre-spike timing supervision but is limited here because the underlying session was already well-fit and the per-window warmup (100 bins) is still far shorter than the slow h-current/adaptation time constants.
- Next step: The larger expected payoff is on weaker sessions and from true cross-window state continuity. Compare this no-mask-pre config on the harder `20260529_025936` session, and treat full stateful continuous-recording simulation (carrying membrane/adaptation/h state across windows) as a separate follow-up rather than relying on a longer fixed warmup alone.

### 2026-05-29 - Convergence (Epochs=40) No-Mask-Pre Voltage-Augmented Run For 20260523_084407
- Goal: Re-run the no-mask-pre voltage-augmented fit on `20260523_084407` with a higher epoch ceiling and early stopping (instead of the matched 6-epoch budget) to see the converged operating point, since the 6-epoch run was still improving at its last epoch.
- Hypothesis: Letting training converge should improve edge ranking and reduce false negatives; the effect on the non-leaky surrogate-FDR thresholded precision/F1 is unknown.
- Files changed: `LIF-simulation/voltage_augmented_learned_lif_outputs/voltage_augmented_learned_lif_20260523_084407_cmp1200s_voltage_postcentered_k100_exclbursts_allrec_nomaskpre_ep40.pt`, `LIF-simulation/voltage_augmented_learned_lif_outputs/connectivity_20260523_084407_cmp1200s_voltage_postcentered_k100_exclbursts_allrec_nomaskpre_ep40.npz`, `LIF-simulation/voltage_augmented_learned_lif_outputs/voltage_augmented_learned_lif_20260523_084407_cmp1200s_voltage_postcentered_k100_exclbursts_allrec_nomaskpre_ep40.png`, `LIF-simulation/voltage_augmented_learned_lif_outputs/voltage_augmented_learned_lif_20260523_084407_cmp1200s_voltage_postcentered_k100_exclbursts_allrec_nomaskpre_ep40_score_separation.png`
- Parameters changed: matched the 6-epoch no-mask-pre run exactly (`mask_pre_ms=0.0`, `warmup=100`, `K=100`, `batch=128`, `max_delay=8`, `l1=0.01`, `pos_weight=5.0`, `dt=1.0`, hybrid candidates, `threshold_mode=adaptive`, `connectivity_threshold_mode=surrogate_fdr`, `surrogate_fdr=0.005`, `n_threshold_surrogates=4`, `surrogate_epochs=2`, `surrogate_patience=1`, `voltage_lambda=1.0`, `--exclude-detected-bursts`) except `epochs` raised from `6` to `40` with `patience=8`.
- Validation run: Executed the all-recordings CLI on CPU; training early-stopped at epoch `23` (`best val loss=0.7674`, `spike=0.1630`), then completed the 4-surrogate null calibration.
- Key result: Convergence improved ranking and recall but hurt thresholded precision. ep40 vs the 6-epoch run: `AUC=0.9169` (vs `0.9040`), `AP=0.7573` (vs `0.7331`), `sign_accuracy=0.9702` (vs `0.9665`), `weight_corr=0.7910` (vs `0.7495`), `recall=0.7928` (vs `0.6776`), `TP=2127` (vs `1818`, `+309`), `FN=556` (vs `865`, `-309`), but `FP=1457` (vs `628`, `+829`), `precision=0.5935` (vs `0.7433`), `F1=0.6788` (vs `0.7089`), `selected_edges=3584` (vs `2446`). Threshold `0.2064`, `estimated_FDR=0.00495` (target `0.005`).
- Interpretation: The converged model is genuinely better at ranking and sign/weight recovery and strongly reduces false negatives, but the circular-shift surrogate-FDR threshold held at target `0.005` admits far more false positives at convergence (actual FP/TP ratio ~0.41 despite estimated FDR ~0.005). This indicates the time-shift null increasingly underestimates the true false-positive rate as the model concentrates scores on structured candidate edges; the bottleneck for the TP-high / FN-low / FP-low goal is now the threshold/null, not model capacity.
- Next step: Keep the 6-epoch run as the precision-matched comparison and this ep40 run as the converged ranking reference. To recover precision at convergence, sweep a stricter `--surrogate-fdr` (e.g. `0.001`) or improve the null model, rather than shortening training; the higher AUC/AP/weight_corr confirm the underlying fit improved.

### 2026-06-02 - Full 20260529_025936 Voltage-Augmented Event-Window Run With Slow States And Per-Neuron Surrogate FDR
- Goal: Run the harder `20260529_025936` all-recordings session with the newly implemented voltage-augmented improvements: no pre-spike voltage masking, longer warmup, optional reduced slow states, detected-burst exclusion, and non-leaky per-postsynaptic-neuron surrogate-FDR connectivity thresholding.
- Dataset: `20260529_025936` contains `9` recordings, `540 s` total duration, `456` neurons, `2841` true structural connections, `171417` spikes, and raw voltage sidecars sampled at `0.1 ms`. Fitting used `dt=1.0 ms`, giving spike/voltage matrices `[456, 540000]`. Saved stimulation-onset bins: `18`. Detected burst windows: `96`; detected excluded bins: `55800`; total excluded bins after combining saved onsets and detected bursts: `55804`.
- Continuous-state attempt: First launched the exact requested full continuous-state configuration (`--training-mode continuous_state`, `--slow-state-mode adaptation_h`, `--continuous-chunk-len 1000`) under tag `cmp540s_voltage_contstate_adapth_k100_exclbursts_allrec_nomaskpre_perneuronfdr`. It loaded successfully and prepared `420` train chunks plus `120` validation chunks, but did not reach epoch 1 in a practical CPU setup-check window, so it was stopped before any scientific metric was produced.
- Completed run: Launched the practical staged full run under tag `cmp540s_voltage_event_adapth_k100_exclbursts_allrec_nomaskpre_perneuronfdr` using `--training-mode event_window`, `--slow-state-mode adaptation_h`, `--connectivity-threshold-mode surrogate_fdr_per_neuron`, `--surrogate-fdr 0.005`, `--n-threshold-surrogates 4`, `--surrogate-epochs 2`, `--surrogate-patience 1`, `--k 100`, `--epochs 40`, `--patience 8`, `--batch 128`, `--max-delay 8`, `--l1 0.01`, `--pos-weight 5.0`, `--voltage-lambda 1.0`, `--mask-pre-ms 0.0`, `--warmup 100`, hybrid candidates (`80` spatial + `20` temporal, lag `1-8`), and `--exclude-detected-bursts`.
- Candidate/window setup: Candidate coverage was `2610/2841 = 91.9%`. Validation strategy was held-out recordings (`7` train, `2` val). Event windows: `109960` train (`54980` positive, `54980` negative) and `23138` validation (`11569` positive, `11569` negative). Model size: `412688` trainable parameters (`45600` weights, `364800` delay logits, `456` bias, `916` membrane/threshold parameters, `916` slow-state parameters).
- Training outcome: Early-stopped at epoch `17`; best validation loss occurred at epoch `9` with `best val loss=0.7551`. Logged trajectory: epoch 1 `conn_AUC=0.7447`, epoch 5 `0.8345`, epoch 10 `0.8250`, epoch 15 `0.8147`. Final saved slow-state summary: slow adaptation gain mean `0.0538`, slow adaptation decay `0.9709`, h-current gain mean `0.0882`, h decay `0.9940`, h activation midpoint `0.9865`, h activation slope `1.7642`.
- Key result: `AUC=0.8268`, `AP=0.4692`, `F1=0.4291`, `precision=0.3270`, `recall=0.6238`, `TP=1628`, `FP=3350`, `FN=982`, `TN=39640`, `selected_edges=4978`, median reported per-neuron threshold summary `0.1893`, `estimated_FDR=0.0000`, `expected_null_selected=0.0`, `sign_accuracy=0.9264`, `predicted_tp_sign_accuracy=0.9975`, `weight_corr=0.6930`, positives in candidate set `2610`, total candidate edges `45600`. Per-neuron thresholding selected edges in `448/456` rows, with finite thresholds for `448` rows.
- Interpretation: The harder 0529 session remains substantially less separable than the 0523 all-recordings reference. The model recovered many true edges (`recall=0.624`) and had good sign recovery on predicted true positives, but precision was low because per-neuron surrogate FDR still selected many false positives (`FP=3350`, actual FP/TP ratio about `2.06`) despite estimated null selections of zero. This suggests the circular-shift surrogate null is still too weak for the structured false-positive modes in this high-spike, burst-excluded session; row-wise calibration prevented a global high-rate-row threshold from dominating but did not make the surrogate null realistic enough.
- Artifacts: `LIF-simulation/voltage_augmented_learned_lif_outputs/voltage_augmented_learned_lif_20260529_025936_cmp540s_voltage_event_adapth_k100_exclbursts_allrec_nomaskpre_perneuronfdr.pt`, matching `.png`, and `connectivity_20260529_025936_cmp540s_voltage_event_adapth_k100_exclbursts_allrec_nomaskpre_perneuronfdr.npz`.
- Next step: For FP reduction on 0529, do not rely on the current circular-shift surrogate alone. Run a stricter per-neuron target (`--surrogate-fdr 0.001` or lower) and/or implement a stronger null such as local spike-time jitter or blockwise/recording-local rate-matched surrogates. For true continuous-state training, use GPU or a redesigned chunking/progress path before attempting another 40-epoch all-recordings run.

### 2026-06-02 - Full 20260523_084407 Voltage-Augmented Event-Window Run With Slow States And Per-Neuron Surrogate FDR
- Goal: Run the stronger `20260523_084407` all-recordings session with the same practical voltage-augmented architecture used on `20260529_025936`: no pre-spike voltage masking, longer warmup, reduced adaptation+h slow states, detected-burst exclusion, and non-leaky per-postsynaptic-neuron surrogate-FDR thresholding.
- Dataset: `20260523_084407` contains `20` recordings, `1200 s` total duration, `461` neurons, `2780` true structural connections, `163482` spikes, and raw voltage sidecars sampled at `0.1 ms`. Fitting used `dt=1.0 ms`, giving spike/voltage matrices `[461, 1200000]`. Saved stimulation-onset bins: `100`. Detected burst windows: `97`; detected excluded bins: `60350`; total excluded bins after combining saved onsets and detected bursts: `60353`.
- Completed run: Launched tag `cmp1200s_voltage_event_adapth_k100_exclbursts_allrec_nomaskpre_perneuronfdr` using `--training-mode event_window`, `--slow-state-mode adaptation_h`, `--connectivity-threshold-mode surrogate_fdr_per_neuron`, `--surrogate-fdr 0.005`, `--n-threshold-surrogates 4`, `--surrogate-epochs 2`, `--surrogate-patience 1`, `--k 100`, `--epochs 40`, `--patience 8`, `--batch 128`, `--max-delay 8`, `--l1 0.01`, `--pos-weight 5.0`, `--voltage-lambda 1.0`, `--mask-pre-ms 0.0`, `--warmup 100`, hybrid candidates (`80` spatial + `20` temporal, lag `1-8`), and `--exclude-detected-bursts`.
- Candidate/window setup: Candidate coverage was `2683/2780 = 96.5%`. Validation strategy was held-out recordings (`16` train, `4` val). Event windows: `172290` train (`86145` positive, `86145` negative) and `43204` validation (`21602` positive, `21602` negative). Model size: `417213` trainable parameters (`46100` weights, `368800` delay logits, `461` bias, `926` membrane/threshold parameters, `926` slow-state parameters).
- Training outcome: Early-stopped at epoch `34`; best validation loss occurred at epoch `26` with `best val loss=0.7501`. Logged trajectory: epoch 1 `conn_AUC=0.8774`, epoch 5 `0.8947`, epoch 10 `0.8960`, epoch 15 `0.9135`, epoch 20 `0.9082`, epoch 25 `0.9016`, epoch 30 `0.8982`. Final saved slow-state summary: slow adaptation gain mean `0.2822`, slow adaptation decay `0.0985`, h-current gain mean `0.1548`, h decay `0.9920`, h activation midpoint `2.3634`, h activation slope `2.3176`.
- Key result: `AUC=0.9011`, `AP=0.6428`, `F1=0.4745`, `precision=0.3366`, `recall=0.8036`, `TP=2156`, `FP=4249`, `FN=527`, `TN=39168`, `selected_edges=6405`, median reported per-neuron threshold summary `0.1773`, `estimated_FDR=0.0000`, `expected_null_selected=0.0`, `sign_accuracy=0.9668`, `predicted_tp_sign_accuracy=0.9991`, `weight_corr=0.7846`, positives in candidate set `2683`, total candidate edges `46100`. Per-neuron thresholding selected edges in `455/461` rows, with finite thresholds for `455` rows.
- Interpretation: The slow-state/per-neuron-FDR architecture recovered many more true edges on the strong 0523 session than the earlier converged global-surrogate voltage run (`TP=2156` vs `2127`, `FN=527` vs `556`, recall `0.8036` vs `0.7928`), but precision and F1 dropped sharply because the row-wise circular-shift surrogate threshold selected a much denser prediction set (`FP=4249`, selected edges `6405`, actual FP/TP ratio about `1.97`) while still estimating zero null selections. This confirms that the current circular-shift surrogate is label-free but too weak as an absolute false-discovery calibrator for structured candidate-edge errors after convergence.
- Artifacts: `LIF-simulation/voltage_augmented_learned_lif_outputs/voltage_augmented_learned_lif_20260523_084407_cmp1200s_voltage_event_adapth_k100_exclbursts_allrec_nomaskpre_perneuronfdr.pt`, matching `.png`, and `connectivity_20260523_084407_cmp1200s_voltage_event_adapth_k100_exclbursts_allrec_nomaskpre_perneuronfdr.npz`.
- Next step: Keep this run as the high-recall 0523 slow-state/per-neuron-FDR reference, not as the precision baseline. For the TP-high / FN-low / FP-low goal, test stricter per-neuron targets (`--surrogate-fdr 0.001` or lower) and stronger nulls such as local jitter or blockwise/rate-matched surrogates before changing model capacity again.

### 2026-06-02 - Full 20260531_041642 Voltage-Augmented Event-Window Run With Slow States And Global Surrogate FDR
- Goal: Run the `20260531_041642` all-recordings session with the practical voltage-augmented event-window architecture while dropping per-neuron FDR and returning to global non-leaky `surrogate_fdr` thresholding.
- Dataset: `20260531_041642` contains `50` recordings, `3000 s` total duration, `453` neurons, `2706` true structural connections, `32373` spikes, and raw voltage sidecars sampled at `0.1 ms`. Fitting used `dt=1.0 ms`, giving spike/voltage matrices `[453, 3000000]`. Saved stimulation-onset bins: `100`. Detected burst windows: `37`; detected excluded bins: `19750`; total excluded bins after combining saved onsets and detected bursts: `19813`.
- Completed run: Launched tag `cmp3000s_voltage_event_adapth_k100_exclbursts_allrec_nomaskpre_globalfdr` using `--training-mode event_window`, `--slow-state-mode adaptation_h`, `--connectivity-threshold-mode surrogate_fdr`, `--surrogate-fdr 0.005`, `--n-threshold-surrogates 4`, `--surrogate-epochs 2`, `--surrogate-patience 1`, `--k 100`, `--epochs 40`, `--patience 8`, `--batch 128`, `--max-delay 8`, `--l1 0.01`, `--pos-weight 5.0`, `--voltage-lambda 1.0`, `--mask-pre-ms 0.0`, `--warmup 100`, hybrid candidates (`80` spatial + `20` temporal, lag `1-8`), and `--exclude-detected-bursts`.
- Candidate/window setup: Candidate coverage was `2347/2706 = 86.7%`. Validation strategy was held-out recordings (`40` train, `10` val). Event windows: `6422` train (`3211` positive, `3211` negative) and `1586` validation (`793` positive, `793` negative). Model size: `409973` trainable parameters (`45300` weights, `362400` delay logits, `453` bias, `910` membrane/threshold parameters, `910` slow-state parameters).
- Training outcome: Early-stopped at epoch `29`; best validation loss occurred at epoch `21` with `best val loss=2.3089`. Logged trajectory: epoch 1 `conn_AUC=0.6347`, epoch 5 `0.6430`, epoch 10 `0.6443`, epoch 15 `0.6455`, epoch 20 `0.6466`, epoch 25 `0.6480`. Final saved slow-state summary: slow adaptation gain mean `0.0059`, slow adaptation decay `0.9663`, h-current gain mean `0.0065`, h decay `0.9915`, h activation midpoint `0.2995`, h activation slope `1.2251`.
- Key result: `AUC=0.6468`, `AP=0.0929`, `F1=0.1825`, `precision=0.1115`, `recall=0.5011`, `TP=1176`, `FP=9367`, `FN=1171`, `TN=33586`, `selected_edges=10543`, global threshold `0.0666`, `estimated_FDR=0.00498`, `expected_null_selected=52.5`, `surrogate_edges_per_model=45300`, `sign_accuracy=0.4921`, `predicted_tp_sign_accuracy=0.9226`, `weight_corr=0.2816`, positives in candidate set `2347`, total candidate edges `45300`.
- Interpretation: Dropping per-neuron FDR avoided the row-wise zero-null-selection failure seen on 0523/0529, but it did not solve 0531 because the fitted score ranking itself is weak on this sparse-spiking session (`AUC=0.647`, `AP=0.093`, sign accuracy near chance). The global circular-shift FDR threshold was very low (`0.0666`) and selected `10543` edges, producing many false positives despite a nominal estimated FDR near target. Here the dominant limitation is low information content and weak edge separability, not only the per-neuron thresholding rule.
- Artifacts: `LIF-simulation/voltage_augmented_learned_lif_outputs/voltage_augmented_learned_lif_20260531_041642_cmp3000s_voltage_event_adapth_k100_exclbursts_allrec_nomaskpre_globalfdr.pt`, matching `.png`, and `connectivity_20260531_041642_cmp3000s_voltage_event_adapth_k100_exclbursts_allrec_nomaskpre_globalfdr.npz`.
- Next step: Do not use this 0531 run as a connectivity-quality baseline. For 0531, first diagnose data quality and candidate observability: spike sparsity, candidate-edge coverage (`86.7%`), voltage variance/noise, firing-rate distribution, and whether excluding detected bursts leaves enough informative postsynaptic events. If rerunning inference, use a much stricter operating point and/or stronger null, but the larger issue is likely the session's sparse activity and weak fitted ranking.

### 2026-06-03 - Burst-Aligned Voltage Check For 20260531_041642
- Goal: Check raw voltage behavior during saved stimulation onsets and detected network-burst windows on the sparse `20260531_041642` session before changing voltage-inference window or exclusion settings.
- Hypothesis: If bursts are carrying useful subthreshold connectivity signal, burst-aligned voltage should show a clear depolarizing population component after spike/reset masking; if they are dominated by resets, inhibition, or post-burst recovery, spike-masked voltage should become more negative during the burst window.
- Files changed: `LIF-simulation/scripts/analyze_burst_voltage.py`, `LIF-simulation/scripts/analyze_connected_pair_psp.py`, `LIF-simulation/SCRIPTS_SUMMARY.md`, `LIF-simulation/docs/SCRIPT_DETAILS.md`, `EXPERIMENT_LOG.md`, `LIF-simulation/voltage_augmented_learned_lif_outputs/burst_voltage_analysis_20260531_041642/*`
- Parameters changed: Analysis only. Burst detection used `activity_bin_ms=100`, `smooth_bins=3`, `threshold=max(mean+3*std, 0.10)`, `min_burst_duration_ms=100`, `merge_gap_ms=150`, `pad_before_ms=100`, `pad_after_ms=250`. Voltage summaries used native `0.1 ms` raw sidecars, baseline window `100 ms`, saved-stimulation response window `650 ms`, and spike masking from each spike bin through `2 ms` after spike.
- Validation run: Ran `python scripts/analyze_burst_voltage.py --session "LIF data/20260531_041642" --output-dir "voltage_augmented_learned_lif_outputs/burst_voltage_analysis_20260531_041642"` after validating the new script CLI and editor diagnostics.
- Key result: The session contained `50` recordings (`3000 s`), `32373` spikes, `100` saved stimulation onsets, and `37` detected network-burst windows. Detected bursts had median duration `550 ms`, median spike count `382`, and median active fraction `0.333`; saved stimulation windows had median spike count `203` and active fraction `0.173`. Spike-masked voltage became more negative during bursts rather than depolarizing: detected bursts shifted from `-61.098 mV` baseline to `-61.631 mV` during (`-0.533 mV`), and saved stimulation shifted from `-61.142 mV` to `-61.574 mV` (`-0.432 mV`). During-window fraction at or above `-50 mV` was effectively zero after spike masking.
- Interpretation: In this 0531 session, burst periods are not sustained subthreshold depolarizing plateaus in the population voltage. The observed burst-aligned voltage is dominated by spiking/reset/recovery dynamics plus net hyperpolarization after spike masking, which supports excluding detected bursts for connectivity inference rather than treating them as clean voltage-supervision windows.
- Next step: Use the burst-voltage figures and CSV/JSON summaries as a diagnostic companion to the poor 0531 connectivity run. For PSP-support and delay-window decisions, use connected-pair spike-triggered voltage averages on non-burst windows from a stronger session such as `20260523_084407`, because strict burst exclusion leaves too few usable pair events in 0531.

### 2026-06-06 - 20260604 Voltage-Lambda False-Positive And Stratified Diagnostics
- Goal: Diagnose why the `20260604_023028` voltage-lambda sweep makes false-positive edge calls and where voltage supervision improves recall relative to the lambda-zero ablation.
- Hypothesis: If voltage supervision is adding useful subthreshold information, its recall gains should concentrate in hard strata such as low-presynaptic-rate or weak-weight true edges, and its false positives should be less dominated by common-input, within-cluster, or reverse-edge confounds than the lambda-zero edge set.
- Files changed: `EXPERIMENT_LOG.md`, `fp_diagnostic.py`, `stratified_eval.py`, `LIF-simulation/voltage_augmented_learned_lif_outputs/diagnostics_20260604_023028/*`
- Parameters changed: Post-hoc analysis only. Inputs were the saved all-recordings `20260604_023028` runs `vlam0`, `vlam1`, and `vlam2` with `600 s`, `10` recordings, burst exclusion, `K=100`, `dt=1.0 ms`, `max_delay_ms=10`, `l1=0.005`, global `surrogate_fdr=0.005`, and `10` threshold surrogates.
- Validation run: Ran `python fp_diagnostic.py` on each of the three saved `connectivity_20260604_023028_*vlam{0,1,2}_globalfdr_sur10.npz` files, then ran `python stratified_eval.py --spikeonly <vlam0> --voltage <vlam1> --session "LIF-simulation/LIF data/20260604_023028" --out "LIF-simulation/voltage_augmented_learned_lif_outputs/diagnostics_20260604_023028/stratified_vlam0_vs_vlam1.png"`.
- Key result: FP diagnostics showed that the lambda-zero arm selected far fewer false positives (`FP=181`) but those FPs were highly structured: mean shared parents `2.055` vs TN `0.694` (`2.96x`), within-cluster fraction `0.624` vs TN `0.113` (`5.51x`), and reverse-of-true fraction `0.724`. The `vlam1` arm had more FPs (`1720`) but weaker confound enrichment: mean shared parents `1.401` vs TN `0.670` (`2.09x`), within-cluster fraction `0.276` vs TN `0.109` (`2.54x`), reverse-of-true fraction `0.248`. The `vlam2` arm was similar but denser (`FP=2829`, shared-parent enrichment `2.08x`, within-cluster enrichment `2.63x`, reverse-of-true `0.221`). Stratified `vlam0` vs `vlam1` recall on recoverable true edges (`2549/2724 = 93.6%`) improved in every weight/rate cell, with the largest marginal gain for low-rate presynaptic neurons (`0.220 -> 0.410`, `+0.190`) and the largest single-cell gains in low-rate low/mid-weight edges (`+0.216`, `+0.222`).
- Interpretation: Lambda-zero gives the best thresholded F1 in the aggregate, but its few false positives are strongly concentrated in common-input, within-cluster, and reverse-direction confounds. Lambda-one is less conservative and introduces more false positives, yet its errors are less dominated by those graph confounds and it improves recall broadly, especially for low-presynaptic-rate true edges. This supports treating voltage supervision as useful for hard-edge recovery, while still needing a better graph/threshold constraint to control the denser predicted edge set.
- Next step: Use the diagnostic figures to decide between stricter threshold/null calibration and graph-structured constraints. A practical next comparison is `vlam1` with a stricter global FDR target or a stronger surrogate null, plus a Dale/graph-constraint prototype to specifically suppress reverse-edge and common-input false positives.

### 2026-06-07 - Dale-Constrained Voltage Inference Probe For 20260604_023028
- Goal: Test whether tying each presynaptic neuron's outgoing signs in the voltage-augmented fitted model reduces reverse-edge and within-cluster/common-input false positives on the strong `20260604_023028` session.
- Hypothesis: A Dale-style sign constraint should preserve or improve threshold-free AUC/AP while reducing false-positive enrichment for within-cluster pairs, shared parents, and reverse-of-true direction errors.
- Files changed: `LIF-simulation/lif_inference/voltage_augmented_learned_lif_connectivity.py`, `LIF-simulation/SCRIPTS_SUMMARY.md`, `EXPERIMENT_LOG.md`, `LIF-simulation/voltage_augmented_learned_lif_outputs/connectivity_20260604_023028_cmp600s_voltage_event_adapth_k100_exclbursts_allrec_dt1_delay10_l1p005_vlam1_DALE_globalfdr_sur10.npz`, matching `.pt`, `.png`, score-separation figure, and `LIF-simulation/voltage_augmented_learned_lif_outputs/diagnostics_20260604_023028/fp_diagnostic_vlam1_DALE.png`.
- Parameters changed: Added optional `--dale` to the voltage-augmented CLI. The fitted model uses one learned sign logit per presynaptic neuron and per-edge nonnegative magnitudes, so saved `connectivity_matrix[post, pre]` has globally tied outgoing signs for each presynaptic neuron. Surrogate-FDR null models remain the existing unconstrained circular-shift calibration, so the primary comparison is threshold-free metrics and FP composition rather than surrogate-threshold precision/recall. Run settings otherwise matched the 0604 voltage-lambda-1 setup except the command explicitly used `--candidate-max-lag 8`, while the earlier non-Dale `vlam1` run used lag `1-10`; this makes the comparison slightly confounded by a small candidate-set difference.
- Validation run: Compiled `lif_inference/voltage_augmented_learned_lif_connectivity.py`, ran a tiny CPU sign-tying smoke test (`dale_sign_tied=True`, effective weight shape `(20, 6)`), executed the full CPU run under tag `cmp600s_voltage_event_adapth_k100_exclbursts_allrec_dt1_delay10_l1p005_vlam1_DALE_globalfdr_sur10`, verified the saved `.npz` recorded `dale=True`, and confirmed `mixed_presyn_sign_count=0` in the saved connectivity matrix.
- Key result: The Dale-constrained run completed all `40` epochs in `19421 s`, with held-out validation loss `0.5639` (`spike=0.1434`, `voltage=0.4201`) and candidate coverage `2533/2724 = 93.0%`. Threshold-free connectivity quality fell substantially relative to the earlier non-Dale `vlam1` run: Dale `AUC=0.6818`, `AP=0.2610`, `sign_accuracy=0.8370`, `weight_corr=0.5329`; non-Dale `vlam1` had `AUC=0.8459`, `AP=0.6589`, `sign_accuracy=0.8898`, `weight_corr=0.6456`. At the unconstrained surrogate threshold, Dale selected a much denser and weaker edge set (`F1=0.2551`, `precision=0.1773`, `recall=0.4548`, `TP=1152`, `FP=5347`, `FN=1381`) than non-Dale `vlam1` (`F1=0.6031`, `precision=0.5173`, `recall=0.7230`, `TP=1843`, `FP=1720`, `FN=706`). FP composition improved in the intended direction: Dale FP shared-parent enrichment `1.25x` vs non-Dale `2.09x`, within-cluster enrichment `1.21x` vs `2.54x`, and reverse-of-true FP fraction `0.053` vs `0.248`.
- Interpretation: The Dale sign constraint mechanically worked and strongly reduced graph-confounded false-positive composition, but the current soft-tanh sign parameterization harmed score ranking and signed-weight recovery enough that it is not yet a better inference model. The result suggests common-input/reverse-direction errors are real, but a hard or better-initialized Dale constraint may be needed; the present parameterization starts with sign logits near zero and can make effective weights too weak or hard to optimize.
- Next step: Do not replace the non-Dale voltage baseline with this Dale run. If continuing this direction, rerun a cleaner pure ablation with the same candidate lag as the non-Dale baseline (`candidate_max_lag` omitted or set to `10`) and improve the Dale parameterization, for example by initializing presynaptic signs from ground-truth neuron type when available for an oracle diagnostic, or by using a less saturating signed-scale parameterization before investing in conductance-decoder changes.

### 2026-06-30 - P0+P1a Inhibition Coverage And Type-Stratified Threshold Diagnostics For 20260523_084407
- Goal: Execute phases P0 and P1a from `03_inhibition_improvement_plan.md` as pure post-hoc analysis (no model/simulator/default changes, no retrain): decompose the aggregate detection AUC into excitatory-only and inhibitory-only components, measure E-vs-I candidate coverage, quantify how far recovered inhibitory `|W|` sits above its own noise floor, and check whether oracle type-stratified thresholding recovers true-I edges at a matched FDR that pooled thresholding cannot.
- Hypothesis: Per §1, inhibitory detection is near chance because a current-based kernel underestimates voltage-gated IPSPs, so recovered `|W_inhibitory|` is small and overlaps its null even when sign is right; and pooling E and I candidates in one FDR null sets the inhibitory bar by the large excitatory spread. If so, the inhibitory-only AUC should be far below the 0.975-level excitatory-only AUC, the I-edge `|W|` distribution should overlap its true-negative null heavily, and a type-stratified threshold should let more true-I edges clear at matched FDR than the pooled threshold — unless the I signal/null overlap is so severe that no threshold helps.
- Files changed: `scripts/inhibition_diagnostics.py` (new analysis script), `EXPERIMENT_LOG.md`, `03_inhibition_improvement_plan.md` (Appendix A prompt archive), and artifacts `LIF-simulation/voltage_augmented_learned_lif_outputs/diagnostics_20260523_084407/inhibition_diagnostics_20260523_084407.csv`, `.../inhibition_signal_vs_null_20260523_084407.png`, `.../inhibition_fdr_power_20260523_084407.png`. No model code, defaults, surrogate machinery, or simulator touched.
- Parameters changed: None (post-hoc only). Input export: `connectivity_20260523_084407_voltage_all20_K100.npz` (voltage-augmented, all 20 recordings, `K=100`, `connectivity_threshold_mode=oracle_f1`, saved pooled threshold `0.28017`; this is the strong `20260523_084407` session — 461 neurons, 2,780 synapses). Ground-truth presynaptic E/I type derived from the sign of each presynaptic neuron's outgoing nonzero `true_weights`; per-presynaptic sign consistency asserted (mixed-sign presynaptics = 0). Detection AUC/AP computed on `|connectivity_matrix|`. P1a FDR target = `surrogate_fdr=0.005` to match the pipeline; the per-type null is the empirical `|W|` of true-negative candidates of each type — an APPROXIMATE stand-in that leaks common-input structure, so the FDR figures are a directional check only (the calibrated circular-shift per-type null is P1b).
- Validation run: `python scripts/inhibition_diagnostics.py --conn "LIF-simulation/voltage_augmented_learned_lif_outputs/connectivity_20260523_084407_voltage_all20_K100.npz"` (Python 3.12 `.venv`, numpy 2.4.6 / scikit-learn 1.x / matplotlib Agg). Ran to completion; wrote the CSV and both PNGs to `diagnostics_20260523_084407/`.
- Key result: Typed presynaptics E=358, I=95 (the 8 untyped of the 366 E have no outgoing true edge), mixed-sign = 0 (Dale-consistent). Candidate coverage: E = 0.9693 (2275/2347), I = 0.9423 (408/433); E−I gap = 2.71 points (no large inhibitory coverage gap). Type-restricted detection on `|connectivity_matrix|`: overall AUC 0.9162 / AP 0.7602 (consistent with the 0.916 voltage-augmented benchmark), E-only AUC 0.9747 / AP 0.8917, I-only AUC 0.6337 / AP 0.1300 (I-only is the honest inhibitory number for this run — above the project's standing ~0.48 figure, which is a different run and not mixed in here, but far below E-only). Inhibitory signal-vs-null overlap of recovered `|W|` = 0.7195 (separation AUC 0.6337) versus excitatory overlap 0.1392 (AUC 0.9747): true-I edges sit almost on top of their true-negative-I null. P1a thresholding on the 408 recoverable true-I edges: the existing pooled threshold (0.28017) selects 70 true-I edges but at realized inhibitory FDR 0.6833. The empirical-null realized-FDR floor is 0.0333 pooled and 0.50 for inhibitory candidates (the single highest-`|W|` inhibitory candidate is a false positive), so the matched FDR=0.005 target is UNREACHABLE: at FDR 0.005 both pooled-restricted-to-I and type-stratified-I select 0 true-I edges; even at the loosest sweep target FDR≤0.50 the stratified-I path yields only 3 true-I edges. Across the FDR–power sweep, pooled-restricted-to-I rides at realized I-FDR 0.65–0.83 (14→48→72→98 true-I edges as the pooled target loosens 0.05→0.50), while stratified-I cannot get below its 0.50 floor.
- Interpretation: Decision gate (a) — is there an inhibitory candidate-coverage gap >10 points (→ P6 jumps ahead of P3)? NO. Inhibitory coverage (94.2%) is only 2.71 points below excitatory (96.9%); the bottleneck is not the candidate screen on this session, so P6 stays low-priority. Decision gate (b) — does oracle type-stratification recover meaningful true-I edges at tolerable FDR (→ P1b worth doing)? NO. The inhibitory `|W|` signal overlaps its own null almost completely (overlap 0.72, separation AUC 0.634) and the realized inhibitory FDR floor is 0.50, so no threshold — pooled or type-stratified — selects inhibitory edges at any tolerable FDR; stratification cannot fix a within-type separability problem. This is the §1 diagnosis confirmed at the candidate level: inhibition is recoverable in sign but not in magnitude, because recovered `|W_inhibitory|` is too small/noisy to clear its null. (Caveat, restated: the true-negative null leaks common-input structure, so these are directional, not calibrated, FDR numbers; the high-`|W|` tail of the I-null is partly that leakage — exactly why P1b's principled null is the proper test, but P1a already shows the headroom is small.)
- Next step: Skip P1b — thresholding/stratification is not the lever, since the inhibitory signal does not separate from its null. Deprioritize P6 (no coverage gap). Move to P2 next (reshape the voltage + L1 loss toward inhibition: hyperpolarization-weighted voltage term, subthreshold concentration, asymmetric L1 on inferred-inhibitory candidates — each as an isolated flag-guarded ablation against this same 20260523_084407 export, reporting the I-only AUC/AP decomposition above as the target metric and overall AUC ≥ 0.916 / sign ≥ 0.70 as guardrails). Keep P3 (conductance-based inference synapse) queued as the scientific bet if P2's loss reshaping does not lift the I-only `|W|` separation, since P0 localizes the failure to inhibitory magnitude recovery rather than to candidate coverage or thresholding.

### 2026-06-30 - P2 Loss-Reshaping Flags Implemented + Verified; 20260523_084407 Ablation Runs Blocked On Absent Session Data
- Goal: Implement the two P2 loss-reshaping ablation knobs in the voltage-augmented inference script (hyperpolarization-weighted voltage term; oracle asymmetric L1 on inhibitory candidates), each defaulting to current behavior, then run Ablation B (γ=2) and Ablation A (l1-inhibitory-scale=0, oracle) on the non-depressing `20260523_084407` session and report the P0 inhibitory decomposition against the baseline (I-only AUC up from 0.6337, AP from 0.1300, `|W|`-vs-null overlap down from 0.72) with overall-AUC/sign guardrails.
- Hypothesis: On `20260523_084407` (user-confirmed no short-term depression), the inhibitory magnitude collapse localized by P0 (I-only AUC 0.6337 vs E-only 0.9747; overlap 0.72) is either (i) inhibition present-but-underweighted in the loss — IPSP voltage deflections swamped by large excitatory excursions under a uniform `smooth_l1`, and small inhibitory weights pinned toward zero by uniform L1 — in which case loss reshaping should lift I-only separation; or (ii) a hard limit of the current-based kernel's functional form, in which case loss reshaping will not move I-only separation and the lever is P3. P2 is the cheap test that discriminates these before the P3 rewrite.
- Files changed: `LIF-simulation/lif_inference/voltage_augmented_learned_lif_connectivity.py` (two new CLI flags + both loss functions + epoch-runner plumbing via model attributes + run-pipeline oracle-scale construction + saved-npz provenance), `03_inhibition_improvement_plan.md` (P0/P1a Status lines; new P0b and P5 sections; §2 table + run-order update; Appendix A Prompt 2), `EXPERIMENT_LOG.md`. No simulator, no default behavior change, no surrogate-machinery change.
- Parameters changed: Added `--voltage-hyperpol-gamma` (default 0.0) — weights each valid voltage residual by `1 + γ·relu(−vt)` (normalized, per-neuron baseline-subtracted target) before the `smooth_l1` reduction, implemented as a hyperpolarization-weighted mean `Σ w·ℓ / Σ w` so γ=0 is exactly the prior unweighted mean; type-free. Added `--l1-inhibitory-scale` (default 1.0) — multiplies the L1 penalty by `s` for candidates whose presynaptic neuron is **ORACLE-inhibitory** (sign of summed outgoing `true_weights`), 1.0 otherwise; `s=0` removes L1 pressure on inhibitory candidates. Both configs are attached to the model and read by the shared epoch runners via `getattr` (no signature changes), so surrogate/baseline models that never receive them fall back to the exact prior behavior; the per-type scale is built only when `s≠1.0`. Saved npz now records `voltage_hyperpol_gamma`, `l1_inhibitory_scale`, `l1_inhibitory_type_source`.
- Validation run: (1) Unit tests on both loss functions with synthetic tensors — at defaults the voltage term equals `F.smooth_l1_loss(...)` and L1 equals `weights.abs().mean()` to within 1e-7/1e-9; at γ=2 the voltage term matches the closed-form weighted mean and differs from default; with an L1 scale that zeros a candidate subset the penalty matches `(|W|·scale).mean()` and is below uniform, and scale=ones equals default exactly (event and continuous paths). (2) End-to-end CLI smoke runs on a present voltage session (`20260615_083404`, single recording, 2 epochs, K=20, subsampled): baseline, `--voltage-hyperpol-gamma 2.0` (config line printed; reported voltage loss shifts 0.6523→0.8427), and `--l1-inhibitory-scale 0.0` (oracle path scaled 5391/27000 inhibitory candidate slots, 270/1350 = 20% presynaptics typed inhibitory). (3) A 1-surrogate `surrogate_fdr_per_neuron` smoke with γ=2 active completed, confirming the surrogate-FDR path is unaffected. Saved provenance fields verified on all three exports. Smoke artifacts (present-session, not results) deleted.
- Key result: **Implementation complete and verified; the two scientific ablation runs were NOT executed.** Blocker: session `20260523_084407` raw data (recordings + voltage `.h5` sidecars + `network_*.npz`) is **absent on this machine** — it is gitignored/never synced (`LIF data/` holds only 20260612+ sessions), and only the saved connectivity export from the original fit is present. The P2 ablations require *training* the voltage-augmented model on that session's spike+voltage recordings, which cannot be done without the raw data. No fabricated metrics: I-only AUC/AP and overlap for B/A against the 0523 baseline are therefore not yet measurable here. (Present voltage sessions exist — `20260623_160405` with 20 recordings, `20260615_083404` with 5 — but they are different networks with no P0 baseline, so they cannot be compared to the 0523 I-only 0.6337.)
- Interpretation: The cheap loss-weighting lever (P2) is now fully wired and guardrail-safe (defaults are exact no-ops; surrogate machinery untouched; oracle usage explicitly labeled in flag/tag/log and recorded in the npz), so the discriminating experiment is ready to run the moment the 0523 data is available. Whether loss reshaping lifts I-only separation (→ inhibition present-but-underweighted, keep/extend) or does not (→ current-based form is the ceiling, commit to P3) is the open question this phase will answer once it runs. No claim is made either way yet. The exact commands are: baseline-matched B = `python -m lif_inference.voltage_augmented_learned_lif_connectivity --session "LIF data/20260523_084407" --training-mode event_window --slow-state-mode adaptation_h --connectivity-threshold-mode surrogate_fdr_per_neuron --surrogate-fdr 0.005 --n-threshold-surrogates 4 --surrogate-epochs 2 --surrogate-patience 1 --k 100 --epochs 40 --patience 8 --batch 128 --max-delay 8 --l1 0.01 --pos-weight 5.0 --voltage-lambda 1.0 --mask-pre-ms 0.0 --warmup 100 --exclude-detected-bursts --voltage-hyperpol-gamma 2.0 --output-tag voltage_all20_K100_hyperpolg2`; A appends `--l1-inhibitory-scale 0.0 --output-tag voltage_all20_K100_l1inh0_ORACLE` instead of the γ flag; then `python scripts/inhibition_diagnostics.py --conn <export>` on each for the inhibitory decomposition.
- Next step: User decision required on the run path — (a) restore/point to the `20260523_084407` session data and I run Ablations B then A (multi-hour CPU each; B first, push, A next); or (b) accept a re-baselined proof-of-direction on a present voltage session (`20260623_160405`), running P0 on a fresh fit there first so the P2 deltas are interpretable; or (c) defer the runs and keep the verified flags as the deliverable. Independent of that, the canonical ~0.48-benchmark session's `depressing` flag should be checked separately (P0b applies there if it used STP).
